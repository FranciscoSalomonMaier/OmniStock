import type{Product}from'./product';export interface InventoryBalance{product:Pick<Product,'id'|'sku'|'name'|'unitOfMeasure'|'minimumStock'|'status'|'category'>;currentQuantity:string;reservedQuantity:string;availableQuantity:string;isBelowMinimumStock:boolean;updatedAt:string}export interface Page<T>{data:T[];meta:{page:number;limit:number;total:number;totalPages:number}}export interface Movement{id:string;type:string;quantity:string;currentQuantityBefore:string;currentQuantityAfter:string;reservedQuantityBefore:string;reservedQuantityAfter:string;reason:string;referenceType:string|null;referenceId:string|null;occurredAt:string;product?:Product;performedBy?:{name:string}|null}export interface Reservation{id:string;productId:string;quantity:string;status:string;referenceType:string;referenceId:string;reason:string|null;createdAt:string}
