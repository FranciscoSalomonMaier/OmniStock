import{useState,type FormEvent}from'react';import{Link}from'react-router-dom';import{authService}from'../../services/auth.service';
export function ForgotPasswordPage(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);try{setMessage((await authService.forgotPassword(email)).message)}finally{setLoading(false)}}return <><h1>Recuperar senha</h1><form className="auth-form" onSubmit={submit}><label htmlFor="forgot-email">E-mail</label><input id="forgot-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/>{message&&<p className="notice">{message}</p>}<button disabled={loading}>{loading?'Enviando...':'Enviar instruções'}</button></form><p className="auth-link"><Link to="/login">Voltar ao login</Link></p></>}
