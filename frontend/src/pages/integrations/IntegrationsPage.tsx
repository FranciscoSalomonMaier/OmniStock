import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{useCompany}from'../../hooks/useCompany';import{integrationService}from'../../services/integration.service';import type{ChannelConnection,SalesChannel}from'../../types/integration';
export function IntegrationsPage(){const{activeCompany,activeMembership}=useCompany(),[catalog,setCatalog]=useState<SalesChannel[]>([]),[connections,setConnections]=useState<ChannelConnection[]>([]),[error,setError]=useState('');const canManage=['ADMIN','MANAGER'].includes(activeMembership?.role??'');async function load(){try{const[a,b]=await Promise.all([integrationService.catalog(),integrationService.list()]);setCatalog(a);setConnections(b);setError('')}catch(e){setError(e instanceof Error?e.message:'Falha ao carregar integrações')}}useEffect(()=>{if(activeCompany)void load()},[activeCompany?.id]);async function connect(channel:SalesChannel){const name=window.prompt('Nome para identificar esta conexão',channel.name);if(!name)return;try{await integrationService.create(channel.id,name);await load()}catch(e){setError(e instanceof Error?e.message:'Falha ao criar conexão')}}return <main className="page"><div className="page-title"><div><h1>Integrações</h1><p className="muted">Canais de venda da empresa ativa.</p></div></div>{error&&<p className="form-error">{error}</p>}<h2>Conexões</h2>{connections.length?<div className="cards">{connections.map(x=><Link className="company-card" to={`/integrations/${x.id}`} key={x.id}><strong>{x.displayName}</strong><span>{x.channel.name} · {x.status}</span></Link>)}</div>:<p className="muted">Nenhum canal conectado.</p>}<h2>Canais disponíveis</h2><div className="cards">{catalog.map(channel=><section className="company-card" key={channel.id}><strong>{channel.name}</strong><span>{channel.type}</span><p>{channel.description}</p>{canManage&&<button onClick={()=>void connect(channel)}>Preparar conexão</button>}</section>)}</div></main>}
