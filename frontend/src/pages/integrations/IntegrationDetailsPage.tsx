import{useEffect,useState}from'react';import{Link,useParams}from'react-router-dom';import{useCompany}from'../../hooks/useCompany';import{integrationService}from'../../services/integration.service';import type{ChannelConnection}from'../../types/integration';
export function IntegrationDetailsPage(){const{id=''}=useParams(),{activeMembership}=useCompany(),[data,setData]=useState<ChannelConnection|null>(null),[error,setError]=useState('');const canManage=['ADMIN','MANAGER'].includes(activeMembership?.role??''),isAdmin=activeMembership?.role==='ADMIN';async function load(){try{setData(await integrationService.get(id));setError('')}catch(e){setError(e instanceof Error?e.message:'Falha ao carregar conexão')}}useEffect(()=>{void load()},[id]);async function action(fn:()=>Promise<unknown>){try{await fn();await load()}catch(e){setError(e instanceof Error?e.message:'Operação não concluída')}}return <main className="page"><Link to="/integrations">← Integrações</Link><h1>{data?.displayName??'Conexão'}</h1>{error&&<p className="form-error">{error}</p>}{data&&<><p><strong>Canal:</strong> {data.channel.name}</p><p><strong>Status:</strong> {data.status}</p><p><strong>Credenciais:</strong> acesso {data.hasAccessToken?'configurado':'ausente'}, renovação {data.hasRefreshToken?'configurado':'ausente'}</p><p><strong>Última sincronização:</strong> {data.lastSyncAt??'Nunca executada'}</p>{data.lastError&&<p className="form-error">{data.lastError.message}</p>}{canManage&&<div className="form-actions"><button onClick={()=>void action(()=>integrationService.validate(id))}>Validar</button>{data.status==='DISABLED'?<button onClick={()=>void action(()=>integrationService.enable(id))}>Habilitar</button>:<button onClick={()=>void action(()=>integrationService.disable(id))}>Desabilitar</button>}{isAdmin&&<button className="danger" onClick={()=>window.confirm('Desconectar e apagar as credenciais?')&&void action(()=>integrationService.disconnect(id))}>Desconectar</button>}</div>}</>}</main>}
