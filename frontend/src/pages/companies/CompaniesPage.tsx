import{Link}from'react-router-dom';import{useCompany}from'../../hooks/useCompany';
export function CompaniesPage(){const{companies,activeMembership,selectCompany}=useCompany();return <main className="page"><h1>Empresas</h1>{!companies.length?<p>Nenhuma empresa cadastrada. <Link to="/companies/new">Cadastrar primeira empresa</Link></p>:<div className="cards">{companies.map(item=><button className={item.id===activeMembership?.id?'company-card active':'company-card'} key={item.id} onClick={()=>selectCompany(item.companyId)}><strong>{item.company.tradeName}</strong><span>{item.role}</span></button>)}</div>}<Link to="/companies/new">Nova empresa</Link></main>}
