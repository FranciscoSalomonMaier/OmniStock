import { Link } from 'react-router-dom'
import { useAuth } from '../../hooks/useAuth'
import { useCompany } from '../../hooks/useCompany'
export function DashboardPage(){const{user}=useAuth();const{activeCompany,companies}=useCompany();return <main className="page welcome"><span className="eyebrow">PAINEL</span><h1>Bem-vindo, {user?.name}</h1>{activeCompany?<><p>Você está trabalhando em <strong>{activeCompany.tradeName}</strong>.</p><div className="quick-actions"><Link to="/products">Consultar produtos</Link><Link to="/products/new">Cadastrar produto</Link><Link to="/product-categories">Gerenciar categorias</Link></div></>:<section className="empty-state"><h2>Comece escolhendo uma empresa</h2><p>O dashboard permanece disponível sem empresa ativa. Selecione uma empresa para liberar produtos, categorias e configurações.</p><div className="dashboard-links">{companies.length>0&&<Link className="primary-link" to="/companies">Selecionar empresa</Link>}<Link to="/companies/new">Cadastrar empresa</Link></div></section>}</main>}
