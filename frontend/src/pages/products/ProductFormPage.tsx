import{useEffect,useState,type FormEvent}from'react';import{Link,useNavigate,useParams}from'react-router-dom';import{productService}from'../../services/product.service';import type{Category}from'../../types/product';
const initial={sku:'',name:'',description:'',barcode:'',unitOfMeasure:'UN',costPrice:'',salePrice:'',ncm:'',cest:'',defaultCfop:'',merchandiseOrigin:'',weight:'',height:'',width:'',length:'',minimumStock:'0',status:'ACTIVE',categoryId:''};
export function ProductFormPage(){const{id}=useParams();const navigate=useNavigate();const[form,setForm]=useState(initial);const[categories,setCategories]=useState<Category[]>([]);const[files,setFiles]=useState<File[]>([]);const[error,setError]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{void productService.categories().then(setCategories);if(id)void productService.get(id).then(p=>setForm(Object.fromEntries(Object.keys(initial).map(k=>[k,String((p as unknown as Record<string,unknown>)[k]??'')]))as typeof initial))},[id]);function set(key:string,value:string){setForm({...form,[key]:value})}async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError('');const payload=Object.fromEntries(Object.entries(form).filter(([key,value])=>value!==''||['description','barcode','costPrice','ncm','cest','defaultCfop','merchandiseOrigin','weight','height','width','length','categoryId'].includes(key)).map(([key,value])=>[key,value===''?null:value]));try{const product=id?await productService.update(id,payload):await productService.create(payload);const failures:string[]=[];for(const file of files){try{await productService.upload(product.id,file)}catch{failures.push(file.name)}}if(failures.length){setError(`Produto salvo, mas falhou o upload: ${failures.join(', ')}`);navigate(`/products/${product.id}/edit`,{replace:true});return}navigate(`/products/${product.id}`)}catch(reason){setError(reason instanceof Error?reason.message:'Não foi possível salvar')}finally{setSaving(false)}}const input=(key:string,label:string,type='text')=><label htmlFor={key}>{label}<input id={key} type={type} min={type==='number'?'0':undefined} step={type==='number'?'0.01':undefined} value={form[key as keyof typeof form]} onChange={e=>set(key,e.target.value)}/></label>;return <main className="page"><h1>{id?'Editar produto':'Novo produto'}</h1><form className="product-form" onSubmit={submit}><fieldset><legend>Informações gerais</legend>{input('sku','SKU *')}{input('name','Nome *')}<label>Descrição<textarea value={form.description} onChange={e=>set('description',e.target.value)}/></label>{input('barcode','Código de barras')}<label>Categoria<select value={form.categoryId} onChange={e=>set('categoryId',e.target.value)}><option value="">Sem categoria</option>{categories.filter(c=>c.isActive).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Unidade *<select value={form.unitOfMeasure} onChange={e=>set('unitOfMeasure',e.target.value)}>{['UN','KG','G','L','ML','M','CM','M2','M3','CX','PCT','PAR'].map(u=><option key={u}>{u}</option>)}</select></label></fieldset><fieldset><legend>Preços</legend>{input('costPrice','Preço de custo','number')}{input('salePrice','Preço de venda *','number')}</fieldset><fieldset><legend>Informações fiscais</legend>{input('ncm','NCM (confirme com seu contador)')}{input('cest','CEST')}{input('defaultCfop','CFOP padrão')}<label>Origem<select value={form.merchandiseOrigin} onChange={e=>set('merchandiseOrigin',e.target.value)}><option value="">Não informada</option>{Array.from({length:9},(_,i)=><option key={i} value={i}>{i}</option>)}</select></label></fieldset><fieldset><legend>Peso e dimensões</legend>{input('weight','Peso (kg)','number')}{input('height','Altura (cm)','number')}{input('width','Largura (cm)','number')}{input('length','Comprimento (cm)','number')}</fieldset><fieldset><legend>Estoque mínimo</legend>{input('minimumStock','Estoque mínimo','number')}<small>Será utilizado para alertar quando o saldo estiver abaixo do limite definido.</small></fieldset><fieldset><legend>Imagens</legend><input type="file" multiple accept="image/jpeg,image/png,image/webp" onChange={e=>{const selected=Array.from(e.target.files??[]);const invalid=selected.find(f=>f.size>5*1024*1024);if(invalid)setError(`${invalid.name} excede 5 MB`);else setFiles(selected)}}/><div className="previews">{files.map(f=><span key={f.name}>{f.name}</span>)}</div></fieldset><fieldset><legend>Status</legend><select value={form.status} onChange={e=>set('status',e.target.value)}>{['ACTIVE','INACTIVE','DISCONTINUED'].map(s=><option key={s}>{s}</option>)}</select></fieldset>{error&&<p className="form-error">{error}</p>}<div className="form-actions"><button disabled={saving}>{saving?'Salvando...':'Salvar'}</button><Link to="/products">Cancelar</Link></div></form></main>}
