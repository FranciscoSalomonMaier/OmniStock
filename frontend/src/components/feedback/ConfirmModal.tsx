import type{ReactNode}from'react';export function ConfirmModal({open,title,children,confirmLabel='Confirmar',busy=false,onCancel,onConfirm}:{open:boolean;title:string;children:ReactNode;confirmLabel?:string;busy?:boolean;onCancel:()=>void;onConfirm:()=>void}){if(!open)return null;return <div className="modal-backdrop" role="presentation"><section className="confirm-modal" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><h2 id="confirm-title">{title}</h2><div>{children}</div><div className="form-actions"><button className="secondary" disabled={busy} onClick={onCancel}>Cancelar</button><button disabled={busy} onClick={onConfirm}>{busy?'Processando...':confirmLabel}</button></div></section></div>}
